# Компоненты загрузки и обработки ошибок

## Обзор

Реализованы улучшенные компоненты для отображения состояний загрузки и обработки ошибок с анимациями, контекстными подсказками и возможностью повторных попыток.

## LoadingSpinner

### Описание
Анимированный индикатор загрузки с пульсацией и опциональным затемнением экрана.

### Возможности
- ✅ Пульсирующая анимация (scale 1 → 1.2 → 1)
- ✅ Режим overlay с полупрозрачным фоном
- ✅ Настраиваемый текст и размер
- ✅ Inline-вариант для компактного отображения

### Использование

```tsx
import { LoadingSpinner, InlineSpinner } from '../components/LoadingSpinner';

// Полноэкранный индикатор с overlay
<LoadingSpinner message="Загрузка новостей..." overlay />

// Обычный индикатор
<LoadingSpinner message="Загрузка..." size="small" />

// Компактный inline-индикатор
<InlineSpinner message="Обновление..." color="#007AFF" />
```

### Props

**LoadingSpinner**
- `message?: string` - Текст под индикатором (по умолчанию "Загрузка новостей...")
- `size?: 'small' | 'large'` - Размер индикатора (по умолчанию 'large')
- `overlay?: boolean` - Затемнение фона (по умолчанию false)

**InlineSpinner**
- `message?: string` - Текст рядом с индикатором
- `color?: string` - Цвет индикатора (по умолчанию '#007AFF')

### Анимация
```tsx
const pulseAnim = useRef(new Animated.Value(1)).current;

Animated.loop(
  Animated.sequence([
    Animated.timing(pulseAnim, {
      toValue: 1.2,
      duration: 1000,
      useNativeDriver: true,
    }),
    Animated.timing(pulseAnim, {
      toValue: 1,
      duration: 1000,
      useNativeDriver: true,
    }),
  ])
).start();
```

---

## ErrorView

### Описание
Интеллектуальный компонент для отображения ошибок с контекстными подсказками, анимацией тряски и кнопкой повтора.

### Возможности
- ✅ 4 типа ошибок с уникальными эмодзи и заголовками
- ✅ Анимация тряски при появлении
- ✅ Контекстные советы для каждого типа ошибки
- ✅ Inline-вариант для компактного отображения
- ✅ Кнопка повтора запроса
- ✅ Стилизованные карточки с тенями

### Типы ошибок

#### 1. Network Error (📡)
**Когда**: Проблемы с подключением к интернету
```tsx
<ErrorView errorType="network" onRetry={loadNews} />
```
**Советы**:
- Проверьте подключение к Wi-Fi или мобильным данным
- Попробуйте перезагрузить приложение
- Убедитесь, что у вас есть доступ в интернет

#### 2. Server Error (🔧)
**Когда**: Ошибки сервера (5xx)
```tsx
<ErrorView errorType="server" onRetry={loadNews} />
```
**Советы**:
- Сервер временно недоступен
- Попробуйте повторить попытку через несколько минут
- Если проблема сохраняется, обратитесь в поддержку

#### 3. Not Found Error (🔍)
**Когда**: Ресурс не найден (404)
```tsx
<ErrorView errorType="notFound" onRetry={loadNews} />
```
**Советы**:
- Запрашиваемая страница не найдена
- Проверьте правильность адреса
- Попробуйте вернуться на главную страницу

#### 4. General Error (⚠️)
**Когда**: Общие ошибки
```tsx
<ErrorView errorType="general" message="Что-то пошло не так" onRetry={loadNews} />
```
**Советы**:
- Попробуйте повторить действие
- Перезапустите приложение
- Очистите кеш приложения в настройках

### Использование

```tsx
import { ErrorView, InlineError } from '../components/ErrorView';

// Полноэкранная ошибка с типом
<ErrorView 
  errorType="network" 
  onRetry={() => loadNews()} 
/>

// Ошибка с кастомным сообщением
<ErrorView 
  errorType="general"
  message="Не удалось загрузить данные"
  details="Попробуйте позже"
  onRetry={() => loadNews()}
/>

// Компактная inline-ошибка
<InlineError 
  message="Ошибка загрузки" 
  onRetry={() => loadNews()} 
/>
```

### Props

**ErrorView**
- `errorType?: 'network' | 'server' | 'notFound' | 'general'` - Тип ошибки (определяет эмодзи, заголовок и советы)
- `message?: string` - Основное сообщение об ошибке
- `details?: string` - Дополнительные детали ошибки
- `onRetry?: () => void` - Callback для кнопки "Повторить попытку"

**InlineError**
- `message: string` - Сообщение об ошибке
- `onRetry?: () => void` - Callback для ссылки "Повторить"

### Анимация тряски
```tsx
const shakeAnim = useRef(new Animated.Value(0)).current;

Animated.sequence([
  Animated.timing(shakeAnim, {
    toValue: 10,
    duration: 100,
    useNativeDriver: true,
  }),
  Animated.timing(shakeAnim, {
    toValue: -10,
    duration: 100,
    useNativeDriver: true,
  }),
  // ... еще 2 раза
  Animated.timing(shakeAnim, {
    toValue: 0,
    duration: 100,
    useNativeDriver: true,
  }),
]).start();
```

---

## Интеграция в проект

### NewsListScreen

```tsx
const [error, setError] = useState<'network' | 'server' | 'notFound' | 'general' | null>(null);

const loadNews = async () => {
  try {
    setError(null);
    // ... загрузка данных
  } catch (err: any) {
    let errorType: 'network' | 'server' | 'general' = 'general';
    
    if (err.message?.includes('Network') || err.code === 'ECONNABORTED') {
      errorType = 'network';
    } else if (err.response?.status >= 500) {
      errorType = 'server';
    }
    
    setError(errorType);
  }
};

// В render:
if (loading && articles.length === 0) {
  return <LoadingSpinner message="Загрузка новостей..." overlay />;
}

if (error && articles.length === 0) {
  return <ErrorView errorType={error} onRetry={() => loadNews()} />;
}
```

### FavoritesScreen

```tsx
if (loading) {
  return <LoadingSpinner message="Загрузка избранного..." overlay />;
}
```

### ArticleDetailScreen

```tsx
// Для inline-сообщений при загрузке изображений
{imageLoading && <InlineSpinner message="Загрузка изображения..." />}

// Для inline-ошибок
{imageError && (
  <InlineError 
    message="Не удалось загрузить изображение" 
    onRetry={loadImage} 
  />
)}
```

---

## Стилизация

### LoadingSpinner
```tsx
const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: 'transparent',
  },
  overlay: {
    backgroundColor: 'rgba(0, 0, 0, 0.5)',
  },
  animatedCircle: {
    width: 80,
    height: 80,
    borderRadius: 40,
    backgroundColor: '#FFF',
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: 0.25,
    shadowRadius: 3.84,
    elevation: 5,
  },
  dots: {
    fontSize: 32,
    color: '#007AFF',
    letterSpacing: 4,
  },
  message: {
    fontSize: 16,
    color: '#007AFF',
  },
});
```

### ErrorView
```tsx
const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    padding: 20,
    backgroundColor: '#F5F5F5',
  },
  content: {
    backgroundColor: '#FFF',
    borderRadius: 16,
    padding: 30,
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: 0.1,
    shadowRadius: 3.84,
    elevation: 5,
  },
  emoji: {
    fontSize: 60,
    marginBottom: 15,
  },
  title: {
    fontSize: 22,
    fontWeight: '700',
    color: '#333',
  },
  button: {
    backgroundColor: '#007AFF',
    paddingHorizontal: 30,
    paddingVertical: 14,
    borderRadius: 10,
    shadowColor: '#007AFF',
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: 0.3,
    shadowRadius: 3,
    elevation: 3,
  },
  tipsContainer: {
    backgroundColor: '#F8F9FA',
    borderRadius: 8,
    padding: 15,
  },
  inlineContainer: {
    flexDirection: 'row',
    backgroundColor: '#FFF3CD',
    borderLeftWidth: 4,
    borderLeftColor: '#FFC107',
  },
});
```

---

## Преимущества

### UX улучшения
- **Визуальная обратная связь**: Пользователь всегда знает, что происходит
- **Контекстные подсказки**: Помощь в решении проблем без обращения в поддержку
- **Анимации**: Привлекают внимание и делают интерфейс живым
- **Возможность повтора**: Пользователь может сам попробовать решить проблему

### Технические преимущества
- **Типобезопасность**: TypeScript интерфейсы для всех props
- **Переиспользуемость**: Компоненты легко использовать в разных экранах
- **Гибкость**: Inline-варианты для разных сценариев использования
- **Производительность**: Используется `useNativeDriver` для плавных анимаций

---

## Тестирование

### Проверка LoadingSpinner
1. Запустите приложение
2. Откройте NewsListScreen
3. Потяните список вниз для обновления
4. Наблюдайте пульсирующую анимацию

### Проверка ErrorView
1. Отключите интернет
2. Попробуйте загрузить новости
3. Увидите ошибку типа "network" с соответствующими советами
4. Нажмите "Повторить попытку"
5. Включите интернет и снова нажмите "Повторить попытку"

### Проверка типов ошибок
```tsx
// Имитация разных ошибок
setError('network');  // Проблемы с сетью
setError('server');   // Ошибка сервера
setError('notFound'); // Не найдено
setError('general');  // Общая ошибка
```

---

## Будущие улучшения

- [ ] Добавить скелетон-загрузчик для списков
- [ ] Поддержка прогресс-бара для длительных операций
- [ ] Кастомные темы (светлая/темная)
- [ ] Звуковые уведомления при ошибках
- [ ] Автоматический retry с экспоненциальной задержкой
- [ ] Метрики ошибок для аналитики
