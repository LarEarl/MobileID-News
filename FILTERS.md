# Фильтрация новостей

## 🎯 Реализованные функции

### 1. Фильтрация по категориям
- **Все** - все новости
- **Бизнес** - деловые новости
- **Развлечения** - шоу-бизнес и развлечения
- **Общие** - общие новости
- **Здоровье** - медицина и здоровье
- **Наука** - научные открытия
- **Спорт** - спортивные новости
- **Технологии** - IT и технологии

### 2. Фильтрация по датам
- **Все время** - без ограничений по дате
- **Сегодня** - новости за текущий день
- **За неделю** - последние 7 дней
- **За месяц** - последние 30 дней

### 3. Сортировка
- **По дате** - самые свежие новости первыми
- **По релевантности** - наиболее релевантные к запросу
- **По популярности** - самые популярные статьи

## 🎨 UI компоненты

### FilterModal
Модальное окно с фильтрами:
- Красивый дизайн с rounded corners
- Chips для выбора опций
- Счетчик активных фильтров
- Кнопки "Сбросить" и "Применить"

### Кнопка фильтров
- Показывает количество активных фильтров в badge
- Расположена под поисковой строкой
- Эмодзи иконка для визуальной привлекательности

## 📝 Как использовать

### Для пользователя:
1. Откройте приложение
2. Нажмите на кнопку "🔍 Фильтры"
3. Выберите нужную категорию
4. Выберите период времени
5. Выберите тип сортировки
6. Нажмите "Применить"

### Сброс фильтров:
- Нажмите "Сбросить" в модальном окне
- Или выберите "Все" для каждого типа фильтра

## 🔧 Техническая реализация

### Файлы:
- `src/components/FilterModal.tsx` - компонент модального окна
- `src/services/newsService.ts` - обновлен для поддержки фильтров
- `src/screens/NewsListScreen.tsx` - интеграция фильтров

### Типы:
```typescript
interface FilterOptions {
  category: string;
  dateRange: 'all' | 'today' | 'week' | 'month';
  sortBy: 'publishedAt' | 'relevancy' | 'popularity';
}

interface NewsFilters {
  query?: string;
  category?: string;
  fromDate?: string;
  toDate?: string;
  sortBy?: 'publishedAt' | 'relevancy' | 'popularity';
}
```

### Логика фильтрации:
1. Категории применяются к `top-headlines` endpoint
2. Даты и сортировка используют `everything` endpoint
3. Фильтры автоматически перезагружают список при изменении
4. При наличии поискового запроса - приоритет у поиска

## ✨ Особенности

- ✅ Автоматическая перезагрузка при изменении фильтров
- ✅ Визуальный индикатор активных фильтров
- ✅ Плавные анимации модального окна
- ✅ Адаптивный дизайн для разных экранов
- ✅ Сохранение состояния при навигации
- ✅ Поддержка Android и iOS

## 🚀 Будущие улучшения

Возможные дополнения:
- Сохранение выбранных фильтров в AsyncStorage
- Создание пресетов фильтров
- Фильтрация по источникам новостей
- Фильтрация по языку
- История использованных фильтров
